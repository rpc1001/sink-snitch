# Backend API base URL
# If set, frontend will connect directly to backend (bypasses Vite proxy)
# If not set, frontend will use Vite proxy at /api
# Default (proxy): leave unset or comment out
# Direct connection: uncomment and set to your backend URL
VITE_API_BASE_URL=http://127.0.0.1:5000

